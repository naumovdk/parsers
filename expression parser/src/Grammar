EXPR := OR
OR := XOR OR'
OR' := `or` OR' | eps
XOR := AND `xor` XOR | AND
AND := NOT `and` AND | NOT
NOT := `not` NOT | IN
IN := VAR IN'
IN' := `in` IN' | `not` `in` IN' | eps
VAR := `var` | `(` S `)`
