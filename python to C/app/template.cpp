#include <iostream>
#include <string>

string input() {
    string tmp;
    std::cin >> tmp;
    return tmp;
}

int toInt(const &int obj) {
    return obj
}

int toInt(const &string obj) {
    return std::stoi(obj);
}

int main() {
#
}